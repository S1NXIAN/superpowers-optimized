# Lite Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `LITE.md` instruction file that enables token-saving caveman communication when included in OpenCode's `instructions` array.

**Architecture:** Single new file at repo root `LITE.md`. Register in `FILE_COPIES` in `lib/constants.mjs` so `setup.mjs` deploys it alongside other managed files. Add structural test assertion.

**Tech Stack:** Markdown (instruction file), Node.js (constants + test)

---

### Task 1: Add `LITE.md` to FILE_COPIES in constants.mjs

**Files:**
- Modify: `lib/constants.mjs` (FILE_COPIES array)

- [ ] **Step 1: Add LITE.md to FILE_COPIES array**

Insert `{ repoRel: 'LITE.md', configRel: 'LITE.md', executable: false }` between the `AGENTS.md` entry and the `scripts/verify-hash.sh` entry.

- [ ] **Step 2: Verify the array shape is correct**

Run: `npm run test:unit`
Expected: All unit tests pass, including FILE_COPIES structure checks.

---

### Task 2: Create `LITE.md`

**Files:**
- Create: `LITE.md` (repo root)

- [ ] **Step 1: Write LITE.md**

Self-contained instruction file with caveman rules. No references to external paths. Covers: mandatory rules, exceptions (security/irreversible ops/ambiguity), usage instructions for enabling/disabling via opencode.json.

---

### Task 3: Add structural test assertion

**Files:**
- Modify: `tests/lib/constants.test.mjs`

- [ ] **Step 1: Add test for LITE.md in FILE_COPIES**

Assert that `FILE_COPIES` contains an entry with `repoRel: 'LITE.md'`, `configRel: 'LITE.md'`, and `executable: false`.

- [ ] **Step 2: Run tests**

Run: `npm test`
Expected: 84 tests pass (83 existing + 1 new).

---

### Task 4: Verify CLI integration

**Files:**
- None (verification-only)

- [ ] **Step 1: Run setup --dry-run**

Expected: Output includes `• Copy LITE.md` in "Files to copy".

- [ ] **Step 2: Run uninstall --dry-run**

Expected: Runs cleanly. Lists `LITE.md` in "Files to remove" if present.
